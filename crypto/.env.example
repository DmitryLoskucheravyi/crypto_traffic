# Telegram
TELEGRAM_BOT_TOKEN=

# Admin seed account (used once on API boot to create/update the admin user)
ADMIN_EMAIL=
ADMIN_PASSWORD=

# Auth
JWT_SECRET=

# Mongo
MONGO_INITDB_ROOT_USERNAME=
MONGO_INITDB_ROOT_PASSWORD=
MONGO_URI=mongodb://<user>:<password>@mongo:27017/crypto?authSource=admin

# Ports
API_PORT=3031
WEB_PORT=3030
ADMIN_PORT=3032

# Used by the web app server-side to reach the API inside the docker network
API_INTERNAL_URL=http://api:3031
